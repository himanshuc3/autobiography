-- NOTE:
-- 1. goose create widgets sql
-- 2. Connect goose to postgres
-- 3. goose postgres "host=localhost port=5432 user=sakamoto password=sakamoto dbname=storyverse sslmode=disable" status/up/down
-- +goose Up
-- +goose StatementBegin
CREATE TABLE widgets (
    id SERIAL PRIMARY KEY,
    color TEXT
);
-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin
DROP TABLE widgets;
-- +goose StatementEnd
