# Storyverse

## Installation instructions

Prerequisites: Installtion of `node >=v22.x` and `npm >= 7.x` to follow ahead.

### Development environment

1. Install dependencies using `npm i`.
2. Start client dev server using `npm run develop`.

UI should be accessible by default on port `8000`.
